This is the directory containing all of the plotting routines used in 26XX.XXXX. When running reach of these files, you can add the argument -r, which is the flag telling the plot scripts to instead use the user-generated data in the repro/ directory.

